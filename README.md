# two-node-monitoring-stack-prometheus-grafana-rhel9
Set up a two-node monitoring system on RHEL 9 where one machine runs Prometheus and Grafana, and the other is the target being monitored. Dealt with real issues like SELinux blocking service execution and firewall rules along the way. Live Grafana dashboard tracks CPU, memory, disk, and network — with alerts that actually fire.
